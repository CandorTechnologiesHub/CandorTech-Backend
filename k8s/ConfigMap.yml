apiVersion: v1
kind: ConfigMap
metadata:
  name: candorapi-config
  namespace: candorapi
data:
  SPRING_PROFILES_ACTIVE: "prod"
  CORS_ALLOWED_ORIGINS: "https://candorapi.lordibe.tech"
  MAIL_PROVIDER: "javamail"
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  FRONTEND_RESET_PASSWORD_URL: "https://localhost:3000/reset-password"
  JWT_EXPIRATION_MS: "3600000"